-r requirements.txt

# LLM/tool evaluation framework used by tests marked with @pytest.mark.deepeval.
# Newer DeepEval releases require pydantic>=2.11.7/httpx>=0.28.1; the app is
# currently pinned to pydantic==2.9.2 and httpx==0.27.2.
deepeval==2.4.0
